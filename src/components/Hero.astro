---
interface Props {
	className?: string;
}

const { className } = Astro.props;
---

<div class="hero min-h-screen">
	<main class:list={["hero-content text-center max-w-3xl", className]}>
		<slot />
	</main>
</div>
