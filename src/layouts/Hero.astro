---
interface Props {
	className?: string;
}

const { className } = Astro.props;
---

<div class="hero min-h-screen ljcontent">
	<div class:list={["hero-content text-center max-w-3xl", className]}>
		<slot />
	</div>
</div>
