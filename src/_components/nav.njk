<nav class="nav">
    <h1 class="nav__title">lajbel</h1>
    <div class="nav__cards-container">
        {{ comp.cardFolder({ cardTitle: "Home", tag: "about", active: true }) | safe }}
        {{ comp.cardFolder({ cardTitle: "Code Projects", tag: "project" }) | safe }}
        {{ comp.cardFolder({ cardTitle: "Games", tag: "game" }) | safe }}
    </div>
</nav>