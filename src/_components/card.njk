<div id="{{ id }}" class="card" style='
--card-img: url("{{ image }}");
--card-color: {{ color }};
--card-info-opacity: {{ opacity or 0.4 }};
'>
    <div class="card__info">
        <h4 class="card__name">{{ title }}</h4>
        <p class="card__description">{{ description }}</p>
    </div>
</div>